int xorOperation(int n, int start) {
        
        int arr[n],ans;

        for(int i=0;i<n;i++)
        {
            arr[i]=start+2*i;
        }

        ans=arr[0];

        for(int i=1;i<n;i++)
        {
            ans^=arr[i];
        }

        return ans;
    }
