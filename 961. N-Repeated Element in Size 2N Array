 int repeatedNTimes(vector<int>& nums) {
        
        int n=nums.size(),i;
        unordered_map<int,int>mp;
        
        for(i=0;i<n;i++)
        {
            mp[nums[i]]++;
            
            if(mp[nums[i]]>1)
               break;
        }
        
        return nums[i];
    }
