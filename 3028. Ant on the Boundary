int returnToBoundaryCount(vector<int>& nums) {
        
        int count=0,len=0;

        for(int i: nums)
        {
            len+=i;
            if(len==0)
            count++;
        }

        return count;
    }
